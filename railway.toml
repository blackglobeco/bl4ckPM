[build]
builder = "nixpacks"
buildCommand = "npm install -g vercel && pip install -r requirements.txt"
# cache bust 1

[deploy]
startCommand = "export PATH=$(npm root -g)/../bin:/opt/venv/bin:$PATH && python main.py"
