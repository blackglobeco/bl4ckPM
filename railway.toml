[build]
builder = "nixpacks"
buildCommand = "npm install -g vercel && pip install -r requirements.txt"
# cache bust 1

[deploy]
startCommand = "which vercel || find / -name 'vercel' -type f 2>/dev/null | head -5 && python main.py"
